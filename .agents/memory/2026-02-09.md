# Memoria - 2026-02-09

## Contesto

- Progetto: `HelloC64` (KickAssembler, C64/VICE).
- Esercizio corrente: `17_stableRaster.asm`.

## Cose da ricordare

- Preferenza utente: spiegazioni graduali in italiano, senza salti logici.
- Durante E17 e' emerso che la fascia raster con loop lungo dentro IRQ produce sfarfallio sul lato destro.
- Comprensione consolidata sui quiz timing:
  - ciclo loop `DEX/BNE`: `5*N - 1`;
  - formula generale: `X = (R*63 - P + 1) / 5`;
  - limite di `X` a 8 bit: per fasce alte serve doppio loop o catena IRQ.
- Approccio consigliato per fascia stabile ampia: catena a 3 IRQ (`arm/top/bottom`).
- Richiesta utente aggiuntiva: mantenere `README.md` in root con descrizione progetto/corso e link a piano, checklist e appunti in `study/`.
- Nota README: usare link Markdown relativi cliccabili (non solo testo in backtick), compatibili con GitHub web.
- Estensione README richiesta: includere i link ai documenti principali in `doc/`, in particolare memory map C64, frequenze PAL e documentazione `SID Factory II`.
- Richiesta su `agents.md`: aggiungere `SID Factory II` all'ambiente di sviluppo e includere link a risorse/documentazione (web + file locali in `doc/SID Factory II`).
- Nuova regola progetto: introdurre `config-template.yaml` (versionato) e `config.yaml` (locale, non versionato) con `language` e `notes_backup_path`; tradurre `agents.md` e `README.md` in inglese.
- Nuova regola didattica: `study/course-plan.md` deve essere un vero piano corso dettagliato orientato a portare a sviluppo giochi C64 in Assembly; il progresso studente va solo in `study/course-progress.md`; lingua di entrambi = `language` in `config.yaml`.
- Applicazione immediata: rinominato `study/checklist.md` in `study/course-progress.md` e aggiornato `study/course-plan.md` come piano didattico puro (senza avanzamento studente).
- Nuova convenzione repository: tutti gli esercizi devono risiedere in `exercises/` con naming `exercises/<n>_<exerciseName>.asm`.
- Correzione Git su Windows: rinomina cartella documentazione forzata da `Doc` a `doc` tramite rename in due passaggi per renderla visibile su GitHub.
- Regola linguistica globale: usare sempre la lingua di `config.yaml` per chat, spiegazioni, piani, documenti `study/*` e messaggi di commit.
- README: rendere esplicito il workflow di bootstrap per nuovo studente (uso LLM CLI preferito, opzione clean start con rimozione `exercises/`, reset `.agents/memory`, reset `study/course-progress.md` e rigenerazione `study/course-plan.md` via agente).
- Config aggiornata: `argument` confermato come campo obbligatorio; rimossi `workspace_mode` e `llm_cli` da template e documentazione.
